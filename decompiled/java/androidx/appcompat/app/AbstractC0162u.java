package androidx.appcompat.app;

import android.content.res.Configuration;
import java.util.Locale;

public abstract class AbstractC0162u {
    public static void m466a(Configuration configuration, Locale locale) {
        configuration.setLayoutDirection(locale);
    }

    public static void m467b(Configuration configuration, Locale locale) {
        configuration.setLocale(locale);
    }
}
